import { LoginWithGoogleButton } from "./style"

export default function LoginWithGoogle({ onClick }) {

    return (
        <LoginWithGoogleButton onClick={onClick}>
            Iniciar con Google
        </LoginWithGoogleButton>
    )
}
